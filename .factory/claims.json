[
  {
    "id": "shared-tv-phone-round",
    "claim": "Party games work on one shared TV with a phone-optional player.",
    "where": "landing first screen, README",
    "test": "npm run test:browser -- --grep @claim:shared-tv-phone-round",
    "sandbox": "Fresh TV-sized host and 390 px phone contexts create a real room, join with a shared phone, draw on the TV, and complete a Pass & Guess score."
  },
  {
    "id": "join-code-path",
    "claim": "Use the code shown on the TV or its join page to join a room.",
    "where": "landing join section and host room screen",
    "test": "npm run test:browser -- --grep @claim:join-code-path",
    "sandbox": "Fresh host and phone contexts create a room, follow the rendered join-page URL, submit a display name, and assert that player appears on the host."
  },
  {
    "id": "language-light-round",
    "claim": "Choose Spanish instructions or picture prompts for a language-light round.",
    "where": "site metadata, room language selector, README",
    "test": "npm run test:browser -- --grep @claim:language-light-round",
    "sandbox": "Fresh demo context changes the room to Spanish, plays and completes a translated Draw Together round, then verifies a picture-only prompt."
  },
  {
    "id": "art-provenance",
    "claim": "The footer discloses original AI-assisted artwork.",
    "where": "footer",
    "test": "node --test --test-name-pattern @claim:art-provenance scripts/product-contract.test.mjs",
    "sandbox": "Source contract reads the rendered footer, design provenance record, and shipped hero asset."
  },
  {
    "id": "demo-sandbox",
    "claim": "Try it with sample data opens a ready Draw Together room in a separate 24-hour sample workspace.",
    "where": "landing first screen, demo banner, README",
    "test": "npm run test:browser -- --grep @claim:demo-sandbox",
    "sandbox": "Fresh desktop contexts cover the one-click /demo action and direct /?demo=1 alias; assert the banner, 24-hour workspace, separate demo: storage, reset, isolation, and Start for real cleanup."
  },
  {
    "id": "account-free-sample",
    "claim": "Try the sample without an account.",
    "where": "landing first-screen facts, Privacy page",
    "test": "npm run test:browser -- --grep @claim:account-free-sample",
    "sandbox": "Fresh unauthenticated desktop browser context opens /demo directly, reaches the ready Draw Together room, and proves the sample request carries no Authorization credential or account step."
  },
  {
    "id": "demo-real-room-isolation",
    "claim": "Sample play never changes a real room.",
    "where": "landing first-screen facts, demo documentation, README",
    "test": "npm run test:browser -- --grep @claim:demo-real-room-isolation",
    "sandbox": "Fresh desktop browser context; exercise the seeded sample and assert its requests never use /api/rooms. Rust integration coverage also queries the database after POST /api/demo and proves real rooms remain empty."
  },
  {
    "id": "offline-reload",
    "claim": "The sample room keeps working offline after the first visit.",
    "where": "demo banner and README",
    "test": "npm run test:browser -- --grep @claim:offline-reload",
    "sandbox": "Fresh mobile browser context at /demo; install the worker, clear HTTP cache, go offline, reload, and assert the rendered sample room and cached JS/CSS."
  },
  {
    "id": "same-origin-requests",
    "claim": "A sample lobby visit sends requests only to Living Room Lobby.",
    "where": "Privacy page and README",
    "test": "npm run test:browser -- --grep @claim:same-origin-requests",
    "sandbox": "Fresh desktop browser context at /demo; capture every HTTP request through the seeded sample flow and assert each origin is the local product origin."
  },
  {
    "id": "remote-controls",
    "claim": "Use a TV remote to move and choose.",
    "where": "landing remote hint and game command rail",
    "test": "npm run test:browser -- --grep @claim:remote-controls",
    "sandbox": "Fresh desktop browser context at /demo; use ArrowRight to move focus in the command rail and game picker, press Enter on Point Panic, and assert its playable screen opens."
  },
  {
    "id": "shared-phone",
    "claim": "Pass & Guess supports one shared phone.",
    "where": "game catalogue and README",
    "test": "npm run test:browser -- --grep @claim:shared-phone",
    "sandbox": "Fresh mobile browser context at /demo; choose Pass & Guess, enter the shipped shared-phone sample session, and assert the answer-covering Pass the phone screen."
  },
  {
    "id": "family-pack-unavailable",
    "claim": "Family Pack checkout is not available yet; Statue Switch and Colour Chorus remain locked while Draw Together, Point Panic, and Pass & Guess stay free.",
    "where": "landing facts, Family Pack section, README, Terms",
    "test": "npm run test:browser -- --grep @claim:family-pack-unavailable",
    "sandbox": "Fresh desktop browser context starts at /demo then starts for real; assert there is no buy link, the unavailable checkout message is visible, a locked game gives the honest recovery message, and Draw Together still opens."
  },
  {
    "id": "free-game-availability",
    "claim": "Draw Together, Point Panic, and Pass & Guess stay free.",
    "where": "Family Pack section, Terms, README",
    "test": "npm run test:browser -- --grep @claim:free-game-availability",
    "sandbox": "Fresh desktop browser context at /demo; use each core game from the ready sample lobby and assert its playable TV state opens without a license or billing request."
  },
  {
    "id": "player-count-limits",
    "claim": "Draw Together, Point Panic, and Colour Chorus support 2–10 players; Pass & Guess and Statue Switch support 3–12 players.",
    "where": "game cards and demo room game chooser",
    "test": "npm run test:browser -- --grep @claim:player-count-limits && cargo test --locked claim_player_count_limits_are_enforced_at_every_boundary",
    "sandbox": "Fresh demo context checks all displayed ranges and tests both accepted boundaries plus both rejected sides for every free game. The Rust boundary test repeats all five rules used by the real-room API."
  },
  {
    "id": "shared-tv-canvas",
    "claim": "Every connected phone contributes to one TV canvas.",
    "where": "README and Draw Together phone screen",
    "test": "npm run test:browser -- --grep @claim:shared-tv-canvas",
    "sandbox": "Fresh mobile browser context at /demo switches the shipped Asha sample to player mode, draws on the phone pad, switches back to the demo host, and asserts the rendered TV canvas pixels changed."
  },
  {
    "id": "point-controls",
    "claim": "Point Panic shows a tilt option and lets a phone aim with the labeled arrow pad.",
    "where": "README and Point Panic phone screen",
    "test": "npm run test:browser -- --grep @claim:point-controls",
    "sandbox": "Fresh mobile browser context at /demo opens Point Panic as the shipped Asha player, asserts the tilt option and four labeled arrow buttons, presses arrows, and observes the saved pointer position change."
  },
  {
    "id": "room-retention",
    "claim": "A real room stores its display names, game actions, and scores for up to six hours, then expires.",
    "where": "Privacy page",
    "test": "cargo test --locked claim_real_room_retention_expires_after_six_hours",
    "sandbox": "Isolated in-memory SQLite database with a room one second older than six hours and a room exactly six hours old; run cleanup and assert only the older room is removed."
  },
  {
    "id": "no-advertising-or-analytics",
    "claim": "The app does not load advertising or analytics scripts.",
    "where": "Privacy page",
    "test": "npm run test:browser -- --grep @claim:no-advertising-or-analytics",
    "sandbox": "Fresh desktop browser context at /demo records every request and script source, asserting each is same-origin."
  },
  {
    "id": "minimal-join-data",
    "claim": "The join form asks only for a display name and play mode.",
    "where": "Privacy page",
    "test": "npm run test:browser -- --grep @claim:minimal-join-data",
    "sandbox": "Fresh desktop browser context starts at /demo then starts for real, opens Join a room, and asserts the exact code, display-name, and mode controls with no personal-data fields."
  },
  {
    "id": "license-status",
    "claim": "An inactive stored Family Pack license stays locked and shows a visible status; its check goes directly to Sociobot, not the room API.",
    "where": "Family Pack section, Privacy page, Terms",
    "test": "npm run test:browser -- --grep @claim:license-status",
    "sandbox": "Fresh desktop browser context starts at /demo, then visits the landing page with an inactive fixture license; intercept the documented Sociobot verification response, assert the token is stripped from the URL and cached locally, the quiet inactive notice is visible, games remain locked, and no room API request carries the check."
  }
]
